# explain01.py:

```python
countar = 1 # Initialize the countar

while countar < 71: # Iterate the loop 70 times
    print ("The curent countar value is: ", countar) # Print the curent countar value
    countar = countar + 1 # Increment the countar
```
